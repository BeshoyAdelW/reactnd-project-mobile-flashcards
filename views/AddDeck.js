import React from "react";
import { StyleSheet, Text, View, StatusBar } from "react-native";
import globalStyles from "../utils/globalStyles";

export default function AddDeck() {
  return (
    <View styles={globalStyles}>
      <Text>Add deck</Text>
    </View>
  );
}
